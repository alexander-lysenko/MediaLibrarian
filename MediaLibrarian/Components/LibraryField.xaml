<UserControl x:Class="MediaLibrarian.Components.LibraryField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="55" d:DesignWidth="480">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="3*" MaxWidth="250" />
            <ColumnDefinition Width="32" MaxWidth="32" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="Auto" MaxHeight="20" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Grid.Column="0" Padding="0">
            <TextBox VerticalContentAlignment="Center" x:Name="FieldName"></TextBox>
        </Border>
        <Border Grid.Row="0" Grid.Column="1" Padding="5,0">
            <ComboBox x:Name="FieldType"></ComboBox>
        </Border>
        <Border Grid.Row="0" Grid.Column="2" Padding="0">
            <Button ToolTip="Это поле нельзя удалить">-</Button>
        </Border>
        <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3">
            <TextBlock VerticalAlignment="Center">Validation Message</TextBlock>
        </Border>
    </Grid>
</UserControl>