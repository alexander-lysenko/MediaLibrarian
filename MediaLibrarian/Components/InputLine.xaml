<UserControl x:Class="MediaLibrarian.Components.InputLine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Name="LayoutRoot"
             mc:Ignorable="d"
             d:DesignWidth="480"
             d:DesignHeight="48">
    <Grid DataContext="{Binding ElementName=LayoutRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Padding="0,0,0,2">
            <TextBlock Style="{DynamicResource Ellipsis}" Text="{Binding Label}" />
        </Border>
        <Border Grid.Row="1" Padding="0,0,0,4">
            <TextBox Margin="0,0,0,4" Text="{Binding Value}" />
        </Border>
    </Grid>
</UserControl>